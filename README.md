All ".mat" data files should be combined into a single folder, these include the folders named VLF Data and other .mat data files outside the VLFdata folders. 
Change the directories in the codes accordingly. 
